export enum RoomStatus {
  InLobby = "InLobby",
  InGame = "InGame",
  Results = "Results",
}

export enum RoomRoundsType {
  Both = "Both",
  Song = "Song",
  Artist = "Artist",
}

export enum RoundStatus {
  Playing = "Playing",
  Results = "Results",
}

export enum RoundType {
  Song = "Song",
  Artist = "Artist",
}
