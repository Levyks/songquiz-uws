export enum RoomStatus {
  InLobby = "InLobby",
  InGame = "InGame",
  Results = "Results",
}

export enum RoundStatus {
  Playing = "Playing",
  Results = "Results",
}

export enum RoundType {
  Song = "Song",
  Artist = "Artist",
}
